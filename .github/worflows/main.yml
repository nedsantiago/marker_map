steps:
  - name: Applying Secrets
    with: # Set the secret as an input
      super_secret: ${{ secrets.SUPABASE_URL }}
    env: # Or as an environment variable
      super_secret: ${{ secrets.SUPABASE_ANON_KEY }}

